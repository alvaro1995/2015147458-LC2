@model IEnumerable<_2015147458_ENT.TipoTrabajador>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeroTipoTrabajador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroTipoTrabajador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoTrabajadorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoTrabajadorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoTrabajadorId })
        </td>
    </tr>
}

</table>
