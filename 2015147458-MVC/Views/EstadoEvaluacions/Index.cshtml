@model IEnumerable<_2015147458_ENT.EstadoEvaluacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeroEstadoEvakuacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroEstadoEvakuacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadoEvaluacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstadoEvaluacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadoEvaluacionId })
        </td>
    </tr>
}

</table>
